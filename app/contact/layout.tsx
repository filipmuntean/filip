export const metadata = {
	title: "Contact",
	description: "Get in touch with Filip Muntean.",
};

export default function ContactLayout({
	children,
}: { children: React.ReactNode }) {
	return children;
}
